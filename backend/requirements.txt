fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
geoalchemy2==0.15.1
qdrant-client==1.9.1
rasterio==1.3.10
shapely==2.0.4
geopandas==0.14.4
numpy==1.26.4
Pillow==10.3.0
scikit-learn==1.5.0
python-multipart==0.0.9
pydantic==2.7.1
psutil==5.9.8
requests==2.32.3
python-dotenv==1.0.1
langgraph==0.1.19
langchain-community==0.2.7
langchain-ollama==0.1.0

# --- Optional heavy ML deps -------------------------------------------------
# Only required if you stage real RemoteCLIP / Prithvi-EO weights (see
# models/*/README.md). The app runs fully without them, using clearly
# labelled placeholder embedders/feature extractors instead.
torch==2.3.0
open_clip_torch==2.24.0
# terratorch==0.1.1

# --- ONNX Runtime (required for prithvi_int8.onnx fast CPU inference) -------
# onnxruntime is NOT an optional dep when the INT8 ONNX model is staged.
# Intentionally pinned alongside requirements-ml.txt; GPU users should use
# onnxruntime-gpu instead and remove this line.
onnxruntime>=1.18.0
