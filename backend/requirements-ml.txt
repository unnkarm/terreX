# CPU-only build keeps the deployed stack within its 4 GB budget. GPU users
# can replace this line with the matching CUDA wheel for their environment.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.0+cpu
open_clip_torch==2.24.0
timm==1.0.15
einops==0.8.1
onnxruntime==1.18.1
